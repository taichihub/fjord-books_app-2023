<%= link_to t('views.common.new', name: Comment.model_name.human), path %>

<h2><%= t('views.common.title_index', name: Comment.model_name.human) %></h2>

<% comments.each do |comment| %>
  <%= render 'comments/comment', model: comment %>
<% end %>
