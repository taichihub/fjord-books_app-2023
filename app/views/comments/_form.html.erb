<%= form_with(model: @comment, url: form_url, local: true) do |form| %>

  <div class="field">
    <%= form.label :content, t('activerecord.attributes.comment.content') %>
    <%= form.text_area :content, required: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  
<% end %>
